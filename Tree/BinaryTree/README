文件：
BiTree.h -- 二叉树头文件
BiTree.c -- 二叉树函数实现
BiTreeQueue.h -- 二叉树队列头文件
BiTreeQueue.c -- 二叉树队列函数实现
main.c -- 主函数c文件
main.out -- 示例程序
Makefile
Status.h -- 头文件，包含一些状态定义

示例程序：
程序启动时，会要求输入字符序列，先序遍历创建两棵二叉树，空格视为空结点。
输入足够创建一棵树的字符序列后单击回车即可输入另一棵树的创建序列。
创建成功后，程序等待后续的指令，指令包括（打印，先序打印，中序打印，
后序打印，层次打印，复制，镜像，结点数，叶子数，高度，宽度，判断是否二叉排序树，
判断是否完全二叉树，清空，退出）
虽然返回根结点、兄弟结点、双亲结点、祖先结点，查找结点这些操作也实现了，
但是并没有在程序中进行示范。

